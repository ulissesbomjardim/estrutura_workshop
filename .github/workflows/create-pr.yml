name: Dev PRs and sync to Main

permissions:
  contents: write
  pull-requests: write

on:
  pull_request:
    branches: [dev]
    types: [opened, reopened, synchronize]
  push:
    branches: [dev]

jobs:
  test-and-pr:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install "pandas>=2.3.3,<3.0.0" "openpyxl>=3.1.5,<4.0.0"
          python -m pip install "pytest>=8.4.2,<9.0.0" "pytest-cov>=4.0.0,<5.0.0"

      - name: Run tests
        run: python -m pytest -q --maxfail=1

      - name: Create Pull Request to main (only on push to dev)
        if: ${{ github.event_name == 'push' }}
        uses: peter-evans/create-pull-request@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore: sync dev -> main (CI automatic PR)"
          title: "chore: sync dev -> main"
          body: "Automatic PR created by CI after tests passed on dev."
          base: main
          branch: dev
name: Dev PRs and sync to Main

permissions:
  contents: write
  pull-requests: write

on:
  pull_request:
    branches: [ dev ]
    types: [opened, reopened, synchronize]
  push:
    branches: [ dev ]

jobs:
  test-and-pr:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install "pandas>=2.3.3,<3.0.0" "openpyxl>=3.1.5,<4.0.0"
          python -m pip install "pytest>=8.4.2,<9.0.0" "pytest-cov>=4.0.0,<5.0.0"

      - name: Run tests
        run: python -m pytest -q --maxfail=1

      - name: Create Pull Request to main (only on push to dev)
        if: ${{ github.event_name == 'push' }}
        uses: peter-evans/create-pull-request@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore: sync dev -> main (CI automatic PR)"
          title: "chore: sync dev -> main"
          body: "Automatic PR created by CI after tests passed on dev."
          base: main
          branch: dev
name: Dev PRs and sync to Main

permissions:
  contents: write
  pull-requests: write

on:
  pull_request:
    branches: [ dev ]
    types: [opened, reopened, synchronize]
  push:
    branches: [ dev ]

jobs:
  test-and-pr:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install "pandas>=2.3.3,<3.0.0" "openpyxl>=3.1.5,<4.0.0"
          python -m pip install "pytest>=8.4.2,<9.0.0" "pytest-cov>=4.0.0,<5.0.0"

      - name: Run tests
        run: python -m pytest -q --maxfail=1

      - name: Create Pull Request to main (only on push to dev)
        if: ${{ github.event_name == 'push' }}
        uses: peter-evans/create-pull-request@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore: sync dev -> main (CI automatic PR)"
          title: "chore: sync dev -> main"
          body: "Automatic PR created by CI after tests passed on dev."
          base: main
          branch: dev
name: Dev PRs and sync to Main

on:
  pull_request:
    branches: [ dev ]
    types: [opened, reopened, synchronize]
  push:
    branches: [ dev ]

jobs:
  test-and-pr:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install "pandas>=2.3.3,<3.0.0" "openpyxl>=3.1.5,<4.0.0"
          python -m pip install "pytest>=8.4.2,<9.0.0" "pytest-cov>=4.0.0,<5.0.0"

permissions:
  contents: write
  pull-requests: write

      - name: Run tests
        run: python -m pytest -q --maxfail=1

      - name: Create Pull Request to main (only on push to dev)
        if: ${{ github.event_name == 'push' }}
        uses: peter-evans/create-pull-request@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore: sync dev -> main (CI automatic PR)"
          title: "chore: sync dev -> main"
          body: "Automatic PR created by CI after tests passed on dev."
          base: main
          branch: dev
