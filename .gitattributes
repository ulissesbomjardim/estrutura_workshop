# Normalize line endings
* text=auto

## Text files: normalize and store as LF in the repo
# Python and Markdown
*.py    text eol=lf
*.md    text eol=lf

# YAML files (GitHub workflows and other YAML configs) — enforce LF
*.yml   text eol=lf
*.yaml  text eol=lf

# Shell scripts (Unix) -> LF
*.sh    text eol=lf

## Windows scripts: keep CRLF on checkout (useful for PS1/BAT files)
*.ps1   text eol=crlf
*.bat   text eol=crlf

## Binary files (do not attempt to normalize)
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.pdf   binary
*.xlsx  binary
